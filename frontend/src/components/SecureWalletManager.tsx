"use client";

export default function SecureWalletManager() {
  // Temporarily disabled due to AuthContext API changes.
  // This component was referencing methods that are no longer present.
  // Keeping a no-op to avoid build/type errors until it is reworked.
  return null;
}
